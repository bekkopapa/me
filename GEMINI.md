# LLM Wiki Schema (GEMINI.md)

이 문서는 Antigravity(Gemini)가 이 프로젝트의 지식 베이스(Wiki)를 유지 관리하는 방법과 규칙을 정의합니다.

## 위키 구조

- `raw/`: 수정 불가능한 원본 소스 문서 데이터셋.
- `wiki/`: LLM이 생성하고 관리하는 마크다운 문서들.
- `GEMINI.md`: 현재 문서 (스키마 및 워크플로우 가이드).

## 핵심 가이드라인

1. **지속적 축적**: 단순히 정보를 검색(RAG)하는 것을 넘어, 새로운 정보가 들어올 때마다 기존 위키 페이지를 업데이트하고 연결하여 지식을 컴파일합니다.
2. **LLM 전용 소유**: `wiki/` 디렉토리 내의 파일은 원칙적으로 LLM이 작성하고 관리합니다. 사용자는 소스를 제공하고 방향을 지시합니다.
3. **상호 연결**: 모든 새 페이지는 관련 있는 기존 페이지와 양방향 링크(`[[페이지명]]`)를 맺어야 합니다.
4. **일관성 유지**: 새로운 소스가 기존 지식과 충돌할 경우 이를 명시하고 사용자에게 알립니다.

## 주요 워크플로우

### 1. Ingest (소스 흡수)
새로운 소스 파일이 `raw/`에 추가되거나 사용자가 입력을 주면:
- 소스를 분석하여 핵심 요약 페이지 생성.
- 관련 엔티티(인물, 조직, 개념) 페이지 업데이트 또는 생성.
- `wiki/index.md`에 새 항목 추가.
- `wiki/log.md`에 작업 내역 기록.

### 2. Query (질의응답)
사용자가 위키의 지식에 대해 질문하면:
- `wiki/index.md`를 먼저 참조하여 관련 페이지 식별.
- 해당 페이지들을 읽고 답변 구성 (출처 인용 포함).
- 가치 있는 답변이나 분석 결과는 다시 위키 페이지로 박제.

### 3. Lint (상태 점검)
주기적으로 위키의 건강 상태를 점검:
- 고립된 페이지(Orphan pages) 확인.
- 문서 간 모순되는 내용 식별.
- 더 자세한 설명이 필요한 미정의 개념 식별.

## 문서 포맷 규칙

- 모든 위키 파일은 마크다운(`.md`) 형식을 사용합니다.
- 파일 상단에 YAML Frontmatter를 포함할 수 있습니다.
  ```yaml
  ---
  title: 페이지 제목
  created: 2026-04-09
  updated: 2026-04-09
  sources: [source_file_v1.pdf]
  tags: [concept, ai]
  ---
  ```
- 가급적 [[Obsidian]] 스타일의 Wiki Link 문법을 권장합니다.
